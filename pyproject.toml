[tool.poetry]
name = "marcap-data"
version = "0.1.0"
description = ""
authors = ["nsson <starsys7@gmail.com>"]
readme = "README.md"
packages = [{include = "marcap_data"}]

[tool.poetry.dependencies]
python = "^3.11.3"
requests = "^2.32.5"
pandas = "^3.0.0"
playwright = "^1.40.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
